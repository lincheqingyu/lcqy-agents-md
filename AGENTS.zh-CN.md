# 项目 Agent 指令

## 项目范围

- 项目目标：填写项目目标和主要边界。
- 运行环境：填写操作系统、语言版本、运行时和包管理器。
- 技术栈：填写语言、框架、主要依赖和版本事实源。
- 关键目录：填写需要 Agent 重点了解的目录及其职责。
- 关键文件：填写配置、入口、契约和生成文件的位置。
- 真实命令：填写安装、开发、构建、测试、Lint 和发布入口。
- 外部依赖：填写数据库、服务、凭据和本地资源的使用边界。

## 项目约束

- 填写项目特有的业务规则、数据契约、兼容性要求和不可变约束。
- 填写项目已有的架构和代码风格约定。

## 工程原则

- 先理解现有实现，再决定是否新增或重构。
- 保持单一真相源，避免重复实现和文档分叉。
- 优先选择简单、可维护、可验证的方案。
- 修复根因，不用临时补丁和过度兜底掩盖结构性问题。

## 验证与交付

- 行为变化或 Bug 修复：填写需要新增或更新的回归测试。
- 完成后运行与变更相关的最小检查，并如实记录结果。
- 只有在项目约定和用户授权下执行外部写入或 Git 历史操作。
