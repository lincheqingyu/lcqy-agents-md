# 本仓库 Agent 指令

## 目的与范围

本仓库维护可复制的 `AGENTS.md` 规则文档、项目模板和原始参考资料。

根目录的 `AGENTS.md` 只描述本仓库的维护要求和稳定的工程原则。可复用的行为规范、文档规范和技术栈指南放在 [`.agents/`](.agents/)，使用时按项目选择并复制。

## 工程原则

- 先理解现有内容和来源，再新增或重写规则。
- 保持单一真相源。目录索引、规则文档和来源说明不得互相复制出第二套信息。
- 优先选择简单、可维护、可验证的结构，避免无实际收益的抽象和技术债。
- 发现结构性问题时通过重构解决，不用特例、补丁或过度兜底掩盖根因。
- 规则应清晰、可执行、可验证，并明确适用范围。

## 仓库维护

- 修改前阅读 `README.md` 和与任务相关的 `.agents/` 文档。
- `.agents/` 下除 `skills/` 外的 Markdown 是被动资料，不会自动成为项目规则。
- `.agents/skills/` 是 Codex/Pi 的项目 Skill 入口，只放包含有效 `SKILL.md` 的实际 Skill，普通规范不要放入其中。
- `.tmp/references/` 保存原始参考内容，不随意改写其语义；需要提炼时更新 `.agents/` 下的规则文档。
- 新增、移动或重命名资料时，同步更新 `.agents/README.md` 的索引和链接。
- 文档修改完成后检查 Markdown 结构、相对链接和目录是否准确。
- 保留用户已有的未提交变更，不回滚、不覆盖与当前任务无关的内容。

## 交付

- 只提交用户要求范围内的整理和文档变更。
- 如需新增依赖、修改 Git 历史或执行外部写入，先取得明确授权。
- 交付时如实说明实际检查结果、变更范围和未完成事项。
